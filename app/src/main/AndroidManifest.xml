<?xml version="1.0" encoding="utf-8"?>
<manifest package="me.aheadlcx.nicepai"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="me.aheadlcx.plugin.nicepai"/>
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".ui.cate.GoddessAct">


        </activity>
        <activity android:name=".ui.CateDetailAct">

        </activity>

        <activity
            android:name=".ui.catelist.CateMainAct"
            android:theme="@style/AppTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="me.aheadlcx.plugin.nicepai"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".test.TestDaggerAct">

        </activity>
    </application>

</manifest>
